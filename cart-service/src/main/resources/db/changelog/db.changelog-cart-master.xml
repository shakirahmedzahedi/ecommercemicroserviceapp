<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- Create cart table -->
    <changeSet id="1-create-cart-table" author="shakir">
        <createTable tableName="cart">
            <column name="id" type="VARCHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="customer_id" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="total_cart_price" type="DECIMAL(19,2)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- Create cart_item table -->
    <changeSet id="2-create-cart-item-table" author="shakir">
        <createTable tableName="cart_item">
            <column name="id" type="VARCHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="product_id" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="total" type="DECIMAL(19,2)">
                <constraints nullable="false"/>
            </column>
            <column name="cart_id" type="VARCHAR(36)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add foreign key to link cart_item -> cart -->
        <addForeignKeyConstraint
                baseTableName="cart_item"
                baseColumnNames="cart_id"
                constraintName="fk_cart_item_cart"
                referencedTableName="cart"
                referencedColumnNames="id"
                onDelete="CASCADE"/>
    </changeSet>

    <include file="db/changelog/changes/add-quantity-column.xml"/>

</databaseChangeLog>
